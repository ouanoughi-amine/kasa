import React from 'react';
import ReactDOM from 'react-dom/client';
import Path from './src/Path.jsx';


//renvoie le composant Path qui contient le routes.
ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode>
      <Path />
    </React.StrictMode>,
  );